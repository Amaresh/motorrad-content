name: check-seo
on:
  push:
  pull_request:
jobs:
  seo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Working-tree SEO assertions
        run: python3 scripts/check-seo.py
